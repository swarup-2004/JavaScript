let names = ["raj", "kumar", "ajar"];


// Similar to slicing in python
console.log(names.slice(0, 2));

// Replaces the slice with the value provided
names.splice(0, 2, "swarup");

console.log(names);

